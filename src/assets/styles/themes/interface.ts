export interface ITheme {
  background: string;

  borderColor: string;

  textColor: string;

  buttonTextColor: string;
  buttonColor: string;
  buttonHoverColor: string;
  buttonActiveColor: string;
}